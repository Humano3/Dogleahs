Create table admin(
    id int,
    administrador text,
    password text
);

Create table cliente(
    id int,
    name varchar(10),
    apellidos varchar(10),
    email text,
    password text
);

Create table numero(
    id int,
    Mascota varchar(15),Create table numero(
    id int,
    Mascota varchar(15),
    Tipo varchar(10),
    Raza varchar(10),
    Numero varchar(10)
);Create table cliente(
    id int,
    name varchar(10),
    apellidos varchar(10),
    email text,
    password text,
);Create table admin(
    id int,
    administrador text,
    password text,
);Create table admin(
    id int,
    administrador text,
    password text,
);

Create table cliente(
    id int,
    name varchar(10),
    apellidos varchar(10),
    email text,
    password text,
);

Create table numero(
    id int,
    Mascota varchar(15),
    Tipo varchar(10),
    Raza varchar(10),
    Numero varchar(10)
);Create table numero(
    id int,
    Mascota varchar(15),
    Tipo varchar(10),
    Raza varchar(10),
    Numero varchar(10)
);
    Tipo varchar(10),
    Raza varchar(10),
    Numero varchar(10)
);